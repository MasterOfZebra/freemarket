# 🐳 FreeMarket Docker Testing Guide

## Система личных кабинетов - Docker тестирование

Этот гайд поможет вам быстро развернуть и протестировать систему личных кабинетов FreeMarket с помощью Docker.

## 🚀 Быстрый старт

### 1. Автоматический запуск
```bash
# Linux/Mac
chmod +x docker-test.sh
./docker-test.sh

# Windows (в PowerShell)
# Отредактируйте docker-test.sh чтобы использовать docker compose вместо docker_compose_cmd
.\docker-test.sh
```

### 2. Ручной запуск
```bash
# Запуск всех сервисов
docker compose -f docker-compose.test.yml up -d

# Инициализация базы данных (один раз)
docker compose -f docker-compose.test.yml --profile init run --rm init-db
```

## 🌐 Доступ к сервисам

После запуска откройте в браузере:
- **Frontend**: http://localhost:3001
- **Backend API**: http://localhost:8001
- **API документация**: http://localhost:8001/docs
- **Категории API**: http://localhost:8001/v1/categories

## 🧪 Тестирование функциональности

### Автоматическое тестирование API
```bash
# Запуск автоматических тестов API
chmod +x test-docker-api.sh
./test-docker-api.sh
```

### Ручное тестирование через браузер

#### 1. Регистрация пользователя
1. Откройте http://localhost:3001
2. Нажмите "Войти / Регистрация"
3. Заполните форму регистрации:
   - Email: `test@example.com`
   - Пароль: `testpass123`
   - ФИО: `Тестовый Пользователь`
   - Город: `Алматы`
4. Нажмите "Зарегистрироваться"

#### 2. Вход в систему
1. После регистрации система автоматически перейдет к форме входа
2. Введите email и пароль
3. Нажмите "Войти"

#### 3. Личный кабинет
1. После входа нажмите "Личный кабинет"
2. Проверьте отображение:
   - Профиль пользователя
   - Список объявлений (пока пустой)
   - Активные обмены (пока пустые)

#### 4. Создание объявления
1. Нажмите "Заполнить анкету обмена"
2. Выберите тип обмена (постоянный/временный)
3. Заполните пользовательские данные
4. Выберите категории и заполните предметы
5. Нажмите "Найти совпадения"

## 🔧 Сервисы в Docker

| Сервис | Порт | Описание |
|--------|------|----------|
| `postgres` | 5433 | База данных PostgreSQL |
| `redis` | 6380 | Redis для кэширования |
| `backend` | 8001 | FastAPI backend |
| `frontend` | 3001 | React frontend |
| `init-db` | - | Инициализация БД (одноразовый) |

## 🛑 Остановка и очистка

```bash
# Остановить все сервисы и удалить orphan контейнеры
docker compose -f docker-compose.test.yml down --remove-orphans

# Остановить и удалить volumes (данные)
docker compose -f docker-compose.test.yml down --volumes

# Полная очистка
docker compose -f docker-compose.test.yml down --volumes --remove-orphans
docker system prune -f
```

## 🐛 Устранение неполадок

### Backend не запускается
```bash
# Проверить логи
docker compose -f docker-compose.test.yml logs backend

# Проверить здоровье сервиса
docker compose -f docker-compose.test.yml exec backend curl -f http://localhost:8000/health
```

### Frontend не собирается
```bash
# Проверить логи сборки
docker compose -f docker-compose.test.yml logs frontend

# Пересобрать без кэша
docker compose -f docker-compose.test.yml build --no-cache frontend
```

### База данных не инициализируется
```bash
# Запустить инициализацию вручную
docker compose -f docker-compose.test.yml --profile init run --rm init-db

# Проверить подключение к БД
docker compose -f docker-compose.test.yml exec backend python -c "
from backend.database import engine
from sqlalchemy import text
with engine.connect() as conn:
    result = conn.execute(text('SELECT 1'))
    print('Database connection: OK')
"
```

## 📊 Мониторинг

### Просмотр логов
```bash
# Все сервисы
docker compose -f docker-compose.test.yml logs -f

# Конкретный сервис
docker compose -f docker-compose.test.yml logs -f backend
```

### Проверка состояния
```bash
# Статус всех сервисов
docker compose -f docker-compose.test.yml ps

# Использование ресурсов
docker stats
```

## 🔐 Безопасность при тестировании

- JWT секретный ключ: `test_jwt_secret_key_change_in_production_123456789`
- База данных: `freemarket_test` / `testpass123`
- Rate limiting: 5 запросов/5 минут на auth endpoints
- HttpOnly cookies для refresh tokens

## 🎯 Что тестировать

### ✅ Основная функциональность
- [ ] Регистрация пользователя
- [ ] Вход/выход из системы
- [ ] Просмотр личного кабинета
- [ ] Создание объявлений
- [ ] Работа с категориями v6

### ✅ API endpoints
- [ ] `GET /health` - проверка здоровья
- [ ] `GET /v1/categories` - получение категорий
- [ ] `POST /auth/register` - регистрация
- [ ] `POST /auth/login` - вход
- [ ] `GET /user/cabinet` - личный кабинет

### ✅ Безопасность
- [ ] Rate limiting на auth endpoints
- [ ] HttpOnly cookies
- [ ] Token rotation
- [ ] Защита от несанкционированного доступа

## 🚀 Следующие шаги

После успешного тестирования:
1. **Production deployment** - использовать `docker-compose.prod.yml`
2. **CI/CD** - настроить автоматическое тестирование
3. **Мониторинг** - добавить логирование и метрики
4. **Оптимизация** - улучшить производительность

---

**Happy testing! 🎉**
